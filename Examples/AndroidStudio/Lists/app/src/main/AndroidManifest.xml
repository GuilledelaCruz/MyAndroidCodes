<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hypercube.com.lists" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="hypercube.com.lists.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="hypercube.com.lists.SpinnerTest"
            android:label="Spinner" >
        </activity>
        <activity
            android:name="hypercube.com.lists.ListViewTest"
            android:label="ListView" >
        </activity>
        <activity
            android:name="hypercube.com.lists.ListViewImproved"
            android:label="ListViewImproved" >
        </activity>

    </application>

</manifest>
